# 🚀 Настройка Vercel Postgres для ESAMIND

## Шаг 1: Создание базы данных в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект ESAMIND
3. Перейдите в раздел **Storage**
4. Нажмите **Create Database**
5. Выберите **Postgres**
6. Выберите регион (ближайший к вам, например `iad1` для США)
7. Нажмите **Create**

Vercel автоматически:
- Создаст базу данных
- Добавит переменные окружения:
  - `POSTGRES_URL` — прямой URL для подключения
  - `POSTGRES_PRISMA_URL` — URL с connection pooling (используйте этот для Prisma)
  - `POSTGRES_URL_NON_POOLING` — URL без pooling

---

## Шаг 2: Обновление переменных окружения

### В Vercel Dashboard:

Vercel автоматически добавит переменные, но убедитесь что:
- `DATABASE_URL` = `POSTGRES_PRISMA_URL` (для Prisma)
- Или используйте `POSTGRES_PRISMA_URL` напрямую в коде

### Локально (для разработки):

Добавьте в `.env`:
```env
# Используйте POSTGRES_PRISMA_URL из Vercel для production
# Для локальной разработки можно использовать:
# 1. Vercel Postgres (через Vercel CLI)
# 2. Локальный Postgres
# 3. Временно SQLite (но лучше использовать Postgres)

DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"
```

**Для локальной разработки с Vercel Postgres:**
```bash
# Установите Vercel CLI
npm i -g vercel

# Логин
vercel login

# Получите переменные окружения
vercel env pull .env.local
```

---

## Шаг 3: Обновление Prisma Schema

Schema уже обновлен на `postgresql`. Проверьте `prisma/schema.prisma`:

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

---

## Шаг 4: Миграция базы данных

### Вариант A: Новая база (рекомендуется для начала)

```bash
# Создать новую миграцию для Postgres
npx prisma migrate dev --name init_postgres

# Это создаст все таблицы в Postgres
```

### Вариант B: Миграция данных из SQLite (если есть данные)

```bash
# 1. Экспорт данных из SQLite
npx prisma db pull  # если нужно обновить schema из существующей БД

# 2. Создать миграцию
npx prisma migrate dev --name migrate_to_postgres

# 3. Если нужно перенести данные:
# - Экспортируйте данные из SQLite в SQL
# - Импортируйте в Postgres через psql или Prisma Studio
```

---

## Шаг 5: Генерация Prisma Client

```bash
npx prisma generate
```

---

## Шаг 6: Проверка подключения

### Локально:

```bash
# Откройте Prisma Studio
npx prisma studio

# Или проверьте через код
node -e "const { PrismaClient } = require('./app/generated/prisma/client'); const prisma = new PrismaClient(); prisma.\$connect().then(() => console.log('Connected!')).catch(e => console.error(e))"
```

### В Production:

После деплоя на Vercel, проверьте логи:
- Не должно быть ошибок подключения к БД
- API routes должны работать

---

## 🔧 Настройка для локальной разработки

### Вариант 1: Использовать Vercel Postgres локально

```bash
# Получить переменные окружения
vercel env pull .env.local

# Использовать POSTGRES_PRISMA_URL как DATABASE_URL
echo 'DATABASE_URL=$POSTGRES_PRISMA_URL' >> .env.local
```

### Вариант 2: Локальный Postgres

Установите Postgres локально и используйте:
```env
DATABASE_URL="postgresql://postgres:password@localhost:5432/esamind?schema=public"
```

### Вариант 3: Docker Postgres

```bash
docker run --name esamind-postgres \
  -e POSTGRES_PASSWORD=password \
  -e POSTGRES_DB=esamind \
  -p 5432:5432 \
  -d postgres:15

# В .env
DATABASE_URL="postgresql://postgres:password@localhost:5432/esamind?schema=public"
```

---

## 📊 Проверка работы

После настройки проверьте:

1. **Локально:**
   ```bash
   npm run dev
   # Откройте http://localhost:3000
   # Попробуйте войти в Dashboard
   ```

2. **В Production:**
   - Деплой на Vercel
   - Проверьте логи в Vercel Dashboard
   - Попробуйте синхронизировать заказы

---

## 🚨 Troubleshooting

### Ошибка: "Can't reach database server"

- Проверьте, что `DATABASE_URL` правильно установлен
- Для Vercel Postgres используйте `POSTGRES_PRISMA_URL`
- Проверьте, что база создана в Vercel Dashboard

### Ошибка: "relation does not exist"

- Запустите миграции: `npx prisma migrate deploy`
- Проверьте, что schema обновлен на `postgresql`

### Ошибка подключения локально

- Убедитесь, что используете правильный `DATABASE_URL`
- Для Vercel Postgres: используйте `vercel env pull`
- Проверьте firewall/сеть

---

## 📝 Важные замечания

1. **Connection Pooling:**
   - Всегда используйте `POSTGRES_PRISMA_URL` для Prisma
   - Это автоматически настроено Vercel Postgres

2. **Миграции:**
   - В production используйте: `npx prisma migrate deploy`
   - В dev используйте: `npx prisma migrate dev`

3. **Бэкапы:**
   - Vercel Postgres автоматически делает бэкапы
   - Можно настроить дополнительные бэкапы через Vercel Dashboard

4. **Лимиты:**
   - Free tier: 256 MB storage
   - Для вашего проекта этого более чем достаточно

---

## ✅ Чеклист настройки

- [ ] Создана база данных в Vercel Dashboard
- [ ] Обновлен `prisma/schema.prisma` на `postgresql`
- [ ] Запущена миграция: `npx prisma migrate dev`
- [ ] Сгенерирован Prisma Client: `npx prisma generate`
- [ ] Проверено локальное подключение
- [ ] Задеплоено на Vercel
- [ ] Проверена работа в production

---

## 🎉 Готово!

После выполнения всех шагов ваша база данных будет работать на Vercel Postgres.

