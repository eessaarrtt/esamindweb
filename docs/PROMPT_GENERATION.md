# 🤖 Автоматическая генерация промптов

## Как это работает

Система автоматически генерирует шаблоны промптов для новых `productCode`, если кастомный промпт не найден в `PRODUCT_PROMPTS`.

### Приоритет промптов:

1. **Кастомный промпт** (если есть в `PRODUCT_PROMPTS`) — используется в первую очередь
2. **Автоматическая генерация** (через `generatePromptTemplate()`) — используется как fallback

---

## 🎯 Логика генерации

Генератор анализирует:
- **Категорию продукта** (`tarot`, `energy`, `ritual`, `premium`)
- **Ключевые слова в productCode** (например, `yes_no`, `decision`, `love`, `karmic`)

И создает промпт по паттернам существующих промптов.

---

## 📋 Паттерны по категориям

### 🔮 `tarot` (Таро чтения)

**Паттерны:**
- `yes_no` → Yes/No энергетический скан (300-500 слов)
- `decision` → Сравнение двух путей (500-800 слов)
- `future` → Временные периоды (600-900 слов)
- `karmic` / `twin` / `soulmate` → Кармические связи (500-800 слов)
- `shadow` → Работа с тенью (500-800 слов)
- `love` → Отношения и любовь (500-800 слов)
- `career` → Карьера и направление (500-800 слов)
- `soul_purpose` → Предназначение души (500-800 слов)
- **По умолчанию** → 3-карточный спред (400-700 слов)

**Структура:**
- Базовый промпт с стилем Esara
- Информация о клиенте
- Структура чтения (карты, интерпретация)
- Практические рекомендации
- Теплое завершение

---

### ☕ `energy` (Энергетические чтения)

**Паттерны:**
- `aura` → Чтение ауры и энергополя (400-700 слов)
- `channeled` / `intuitive_message` / `higher_self` → Канальные сообщения (400-700 слов)
- `blocked` → Скан блокировок (400-700 слов)
- `guidance` → Ежедневное/еженедельное руководство (300-500 слов)
- `cord` → Энергетические связи (400-700 слов)
- `past_life` → Прошлые жизни (500-800 слов)
- **По умолчанию** → Общее энергетическое чтение (400-700 слов)

**Структура:**
- Базовый промпт
- Информация о клиенте
- Энергетический анализ
- Практические шаги
- Поддерживающие сообщения

---

### ✨ `ritual` (Ритуалы и PDF-гиды)

**Паттерны:**
- `journal` → Шаблон журнала (800-1200 слов)
- `calendar` → Календарь энергий (1000-1500 слов)
- `workbook` / `new_moon` → Рабочая тетрадь (1000-1500 слов)
- `cards` → Аффирмационные карты (800-1200 слов)
- `guide` / `dream` → Руководство/интерпретация (1000-1500 слов)
- **По умолчанию** → Ритуальный гайд (800-1200 слов)

**Структура:**
- Введение в тему
- Подготовка и материалы
- Пошаговые инструкции
- Вариации
- Интеграция и практика

---

### 🪄 `premium` (Премиум пакеты)

**Паттерны:**
- `full_package` → Комбинированный пакет (1200-1800 слов)
- `year_ahead` → Годовой прогноз (2000-3000 слов)
- **По умолчанию** → Премиум чтение (1500-2500 слов)

**Структура:**
- Множественные аспекты (таро + энергия + канал)
- Детальный анализ
- Интеграция всех частей
- Практические шаги

---

## 🔧 Добавление нового продукта

### Вариант 1: Автоматическая генерация (рекомендуется для начала)

1. Добавьте продукт в `lib/products.ts`:
   ```typescript
   new_product_code: {
     code: "new_product_code",
     title: "New Product Title",
     category: "tarot", // или "energy", "ritual", "premium"
   }
   ```

2. Система автоматически сгенерирует промпт на основе:
   - Категории
   - Ключевых слов в `productCode`
   - Паттернов существующих промптов

3. Проверьте результат и при необходимости создайте кастомный промпт.

---

### Вариант 2: Кастомный промпт (для точной настройки)

1. Добавьте продукт в `lib/products.ts` (как выше)

2. Создайте функцию-промпт в `lib/prompts/products.ts`:
   ```typescript
   export function promptNewProduct(input: ReadingInput): string {
     return `
     You are Esara Vance...
     
     Product: "New Product Title".
     
     Client information:
     ...
     
     [Ваш кастомный промпт]
     `
   }
   ```

3. Добавьте в `PRODUCT_PROMPTS`:
   ```typescript
   export const PRODUCT_PROMPTS = {
     ...
     'new_product_code': promptNewProduct,
   }
   ```

---

## 📊 Примеры автоматической генерации

### Пример 1: Новый таро-продукт
```typescript
// lib/products.ts
tarot_new_reading: {
  code: "tarot_new_reading",
  title: "New Tarot Reading",
  category: "tarot",
}
```

**Результат:** Автоматически сгенерируется промпт для 3-карточного таро-чтения (400-700 слов)

---

### Пример 2: Новый энергетический продукт с ключевым словом
```typescript
energy_chakra_balance: {
  code: "energy_chakra_balance",
  title: "Chakra Balance Reading",
  category: "energy",
}
```

**Результат:** Автоматически сгенерируется промпт для энергетического чтения (400-700 слов)

---

### Пример 3: Новый ритуал
```typescript
ritual_new_moon_manifestation: {
  code: "ritual_new_moon_manifestation",
  title: "New Moon Manifestation Ritual",
  category: "ritual",
}
```

**Результат:** Автоматически сгенерируется промпт для ритуального гайда (800-1200 слов)

---

## 🎨 Настройка генератора

Если нужно изменить паттерны генерации, отредактируйте функции в `lib/prompts/generator.ts`:
- `generateTarotPrompt()` — для таро-чтений
- `generateEnergyPrompt()` — для энергетических чтений
- `generateRitualPrompt()` — для ритуалов
- `generatePremiumPrompt()` — для премиум пакетов

---

## ✅ Преимущества

1. **Быстрый старт** — новый продукт работает сразу
2. **Консистентность** — все промпты следуют единому стилю
3. **Гибкость** — можно переопределить кастомным промптом
4. **Масштабируемость** — легко добавлять новые продукты

---

## 📝 Логирование

Система логирует, какой промпт используется:
- `Using custom prompt builder` — используется кастомный промпт
- `No custom prompt found, generating template automatically` — используется автогенерация

Это помогает отслеживать, какие продукты используют автогенерацию и нуждаются в кастомизации.

