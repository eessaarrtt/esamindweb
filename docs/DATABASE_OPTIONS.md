# 🗄️ Варианты базы данных для ESAMIND

## Текущая ситуация

- **Dev**: SQLite (локальный файл `prisma/dev.db`)
- **Production**: Нужен managed database service (SQLite не работает на Vercel)

---

## 📊 Сравнение вариантов

### 1. **Vercel Postgres** ⭐ РЕКОМЕНДУЮ

**Плюсы:**
- ✅ Полная интеграция с Vercel (автоматические переменные окружения)
- ✅ Простая настройка (1 клик в Vercel Dashboard)
- ✅ Бесплатный тариф: 256 MB storage, 60 hours compute/month
- ✅ Автоматические бэкапы
- ✅ Нет необходимости настраивать connection pooling
- ✅ Работает из коробки с Prisma

**Минусы:**
- ⚠️ Привязан к Vercel (но вы уже используете Vercel)
- ⚠️ Меньше контроля над настройками

**Стоимость:**
- Free: до 256 MB
- Pro ($20/мес): 8 GB storage
- Enterprise: по запросу

**Для вашего проекта:** Идеально, если вы уже на Vercel. Простота > гибкость.

---

### 2. **Supabase** ⭐ ОТЛИЧНЫЙ ВЫБОР

**Плюсы:**
- ✅ Бесплатный тариф: 500 MB database, 2 GB bandwidth
- ✅ Postgres с дополнительными фичами (Auth, Storage, Realtime)
- ✅ Отличный UI для управления данными
- ✅ Автоматические бэкапы
- ✅ Хорошая документация
- ✅ Можно использовать вне Vercel

**Минусы:**
- ⚠️ Нужно настраивать connection pooling для serverless
- ⚠️ Дополнительный сервис (но это может быть плюсом)

**Стоимость:**
- Free: 500 MB database
- Pro ($25/мес): 8 GB database
- Team ($599/мес): 32 GB database

**Для вашего проекта:** Отличный выбор, если хотите больше возможностей и независимость от Vercel.

---

### 3. **Neon** ⭐ ДЛЯ SERVERLESS

**Плюсы:**
- ✅ Serverless Postgres (автоматическое масштабирование)
- ✅ Бесплатный тариф: 3 GB storage
- ✅ Автоматический sleep/wake (экономия ресурсов)
- ✅ Отличная интеграция с Vercel
- ✅ Branching (как Git для БД)

**Минусы:**
- ⚠️ Может быть overkill для простого проекта
- ⚠️ Cold starts при sleep

**Стоимость:**
- Free: 3 GB storage
- Launch ($19/мес): 10 GB storage
- Scale ($69/мес): 50 GB storage

**Для вашего проекта:** Хорошо, если планируете активный рост и нужен serverless подход.

---

### 4. **PlanetScale** (MySQL)

**Плюсы:**
- ✅ Отличный UI
- ✅ Branching (как Git)
- ✅ Бесплатный тариф: 5 GB storage
- ✅ Serverless scaling

**Минусы:**
- ⚠️ MySQL вместо Postgres (нужно менять Prisma schema)
- ⚠️ Некоторые ограничения MySQL (нет foreign keys по умолчанию)

**Стоимость:**
- Free: 5 GB storage, 1 billion rows read/month
- Scaler ($29/мес): 10 GB storage

**Для вашего проекта:** Не рекомендую, т.к. нужно менять schema на MySQL.

---

### 5. **Оставить текущий подход** (SQLite dev + Postgres prod)

**Плюсы:**
- ✅ Полный контроль
- ✅ Можно использовать любой Postgres provider

**Минусы:**
- ⚠️ Нужно настраивать Postgres вручную
- ⚠️ Нужно управлять бэкапами
- ⚠️ Нужно настраивать connection pooling
- ⚠️ Больше работы по поддержке

**Для вашего проекта:** Не рекомендую для начала. Можно перейти позже, если понадобится.

---

## 🎯 Моя рекомендация

### Для начала: **Vercel Postgres**

**Почему:**
1. Вы уже на Vercel → максимальная простота
2. Бесплатный тариф достаточен для начала
3. Нет дополнительной настройки
4. Легко мигрировать позже, если понадобится

**Миграция:**
- SQLite → Postgres: Prisma автоматически обработает при изменении `provider` в schema
- Данные: можно экспортировать/импортировать через Prisma

---

### Альтернатива: **Supabase**

**Если:**
- Хотите независимость от Vercel
- Нужен хороший UI для управления данными
- Планируете использовать дополнительные фичи (Auth, Storage)

---

## 📝 План миграции на Vercel Postgres

### Шаг 1: Создать Vercel Postgres

1. В Vercel Dashboard → ваш проект → Storage
2. Нажать "Create Database" → выбрать "Postgres"
3. Выбрать регион (ближайший к вам)
4. Создать

### Шаг 2: Обновить Prisma Schema

```prisma
datasource db {
  provider = "postgresql"  // было: "sqlite"
  url      = env("DATABASE_URL")
}
```

### Шаг 3: Обновить типы (если нужно)

SQLite использует `Int` для ID, Postgres может использовать `BigInt` или `Int` - обычно работает без изменений.

### Шаг 4: Миграция

```bash
# Создать новую миграцию для Postgres
npx prisma migrate dev --name migrate_to_postgres

# Или применить существующие миграции
npx prisma migrate deploy
```

### Шаг 5: Переменные окружения

Vercel автоматически добавит `POSTGRES_URL` и `POSTGRES_PRISMA_URL` в переменные окружения.

Использовать `POSTGRES_PRISMA_URL` для Prisma (уже настроен connection pooling).

---

## 💰 Стоимость для вашего проекта

**Оценка использования:**
- Заказы: ~100-1000 в месяц
- Размер заказа: ~5-10 KB (personalization + readingText)
- **Итого: ~1-10 MB в месяц**

**Вывод:** Бесплатных тарифов более чем достаточно для начала.

---

## 🔄 Когда переходить на другой сервис

**Рассмотрите альтернативы, если:**
- Нужно больше 256 MB (Vercel) или 500 MB (Supabase)
- Нужны дополнительные фичи (Auth, Storage)
- Нужна независимость от Vercel
- Нужен более продвинутый UI для управления данными

---

## ✅ Итоговая рекомендация

**Начните с Vercel Postgres:**
- Простота настройки
- Бесплатно для начала
- Полная интеграция с Vercel
- Легко мигрировать позже

**Переходите на Supabase, если:**
- Нужен лучший UI для управления данными
- Хотите использовать дополнительные фичи
- Нужна независимость от Vercel

